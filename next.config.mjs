/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['source.unsplash.com', 'picsum.photos', 'plus.unsplash.com', 'images.unsplash.com'],
  },
}

export default nextConfig
